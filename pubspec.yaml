name: flutterbase
description: flutterbase
publish_to: "none"
version: 1.3.10+5

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/
    - assets/adi-registration.properties
